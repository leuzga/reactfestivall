import { atom } from 'jotai';

// Define el atom para available games
export const availableGamesAtom = atom([]);

// Define y exporta el atom para drawer open
export const drawerOpenAtom = atom(false);
